var ModuleViewCtrl =
  ['$scope', '$state', '$compile', '$timeout',
   function(sc, $state, $compile, $timeout)
{
  sc.$state = $state;
  $state.go('definition');
  #{rawJS scripts};
  sc.render = $compile(#{toJSON rendered})(sc);
}];
